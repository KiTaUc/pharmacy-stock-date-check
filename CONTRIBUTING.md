# Участие

Перед изменением создайте отдельную ветку, обновите тесты и выполните `python -m unittest discover -s tests -v`.

Проект должен оставаться локальным, не отправлять рабочие записи наружу и не изменять файлы без явной команды пользователя.
